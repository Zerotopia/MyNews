<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mynews">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".controller.activity.SearchResultActivity"
            android:parentActivityName=".controller.activity.SearchActivity"/>
        <activity android:name=".controller.activity.InformationActivity"
            android:parentActivityName=".controller.activity.MainActivity"/>
        <activity
            android:name=".controller.activity.SearchActivity"
            android:parentActivityName=".controller.activity.MainActivity" />
        <activity
            android:name=".controller.activity.WebActivity"
            android:parentActivityName=".controller.activity.MainActivity" />
        <activity
            android:name=".controller.activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".controller.broadcastreciever.Reciever" />
    </application>

</manifest>